name: Hello World

on:
  push:
    branches: ["main"]

jobs:
  log-commits:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say hello
        run: |
            echo "----- Hola Mundo -------"
